# Decision log

Append an entry for any non-trivial design choice. Newest at the bottom. Each entry: context (what prompted it), the decision, what was deliberately not done, and what it relates to.

## DEC-001 — Substrate installed from the starter kit

**Date:** (installation date)
**Status:** Live.

Installed the agentic-starter-kit baseline: the `co` launcher on a pinned model, the `agentic-guard` and `model-pin-guard` hooks, handoff skills, the knowledge repo + index + `mcp_kb` server, and the local team site. Heavier layers (workers, schedulers, comms) are deferred until a real, repeated need appears — that restraint is part of the design.
